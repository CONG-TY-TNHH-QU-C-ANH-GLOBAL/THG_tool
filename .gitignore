# Binary
/scraper
/scraper.exe
*.exe

# Environment (contains secrets!)
.env

# Database
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Dump files
posts_dump*.txt

# Chrome profiles (contain Facebook session cookies!)
data/profiles/

# Backups
data/backups/

# Generated images
data/images/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Build
dist/
.gocache/
.gocache-ci/
.gomodcache-ci/
.local-bin/
data/downloads/
thg-login-downloads/
trim.txt

# Frontend dependencies (never commit node_modules)
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/public/downloads/

# Root node_modules (TypeScript tooling)
node_modules/
package-lock.json

# Agent OS runtime artifacts (patch history, sandbox, backups)
.agentloop/
*.agentbak.*
tmp/

# Local Claude config (may contain prompt tuning / secrets)
.claude/

# AI experiments / internal strategy (not for public repo)
/ai/
out.txt

# TypeScript config (only needed if TS tooling is published)
tsconfig.json

# Build artifacts — auto-regenerate, machine-specific, no review value.
# Untracked from git history in commit a6a6a75+.
frontend/tsconfig.tsbuildinfo
**/__pycache__/
*.pyc
