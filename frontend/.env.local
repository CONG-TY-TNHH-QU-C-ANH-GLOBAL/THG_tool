# Go API backend URL (server-side proxy — used by Next.js rewrites)
API_URL=http://localhost:8080

# Public backend URL — used client-side for direct WebSocket/iframe connections (VNC viewer).
# Must point directly to the Go API server since Next.js cannot proxy WebSockets.
NEXT_PUBLIC_API_URL=http://localhost:8080
